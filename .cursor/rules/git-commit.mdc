---
description: "Git 提交规范：基于 Conventional Commits，按项目阶段分次提交实践层代码"
globs: ["practices/**"]
alwaysApply: false
---

# Git 提交规范（实践层）

> 基于 Conventional Commits 标准，适配 practices/ 下独立项目的分阶段开发场景。每次提交对应路线图中的一个 Phase 或子功能 F，让 git log 成为项目演进的精确编年史。

## 提交信息格式

```
<type>(<scope>): <中文描述>
```

| type | 含义 | 适用场景 |
|------|------|----------|
| `feat` | 新功能 | 新增 Phase / F 功能模块、页面、组件 |
| `fix` | 修复 | 修复 bug、样式问题、类型错误 |
| `refactor` | 重构 | 重构组件、提取 composable、优化代码结构 |
| `style` | 样式 | CSS/Less 调整、布局修改（不影响逻辑） |
| `chore` | 杂项 | 依赖管理、配置文件、构建脚本 |
| `docs` | 文档 | 项目 README、注释补充 |
| `test` | 测试 | 新增或修改测试用例 |
| `perf` | 性能 | 性能优化（虚拟滚动、防抖、懒加载） |

**scope（作用域）**：使用项目目录名，如 `cms-frontend`、`ecommerce-frontend`。

## 分阶段提交策略

每个项目按路线图的 Phase 分阶段提交，每个 Phase 内按 F 功能点拆分多次提交：

```
Phase 1：骨架搭建
  commit 1: feat(cms-frontend): F1.1 项目初始化脚手架搭建
  commit 2: feat(cms-frontend): F1.2 Element Plus 容器布局与侧边栏导航
  commit 3: feat(cms-frontend): F1.3 Axios 封装与请求拦截器
  commit 4: feat(cms-frontend): F1.4 Vue Router 嵌套路由与导航守卫
  commit 5: feat(cms-frontend): F1.5 Pinia 用户 store 与应用 store

Phase 2：管理员与权限
  commit 6: feat(cms-frontend): F2.1 登录页面表单验证与 JWT 存储
  commit 7: feat(cms-frontend): F2.2 管理员列表表格展示与分页
  commit 8: feat(cms-frontend): F2.3 管理员新增/编辑表单对话框
  commit 9: feat(cms-frontend): F2.4 角色管理 CRUD 与权限分配
```

## 提交纪律

### 1. 单项目单提交

每个 practices/ 下的项目是独立"箱子"，提交时只操作当前项目的文件：

```bash
# 正确：只添加当前项目的变更
git add practices/pj-01-cms-frontend/src/views/LoginView.vue
git add practices/pj-01-cms-frontend/src/router/index.ts

# 错误：一次性添加所有项目
git add practices/  # 禁止
```

### 2. 一个提交只做一件事

```
# 正确
feat(cms-frontend): F2.1 登录页面表单验证与 JWT Token 存储

# 错误：混入了不相关的内容
feat(cms-frontend): F2.1 登录页面 + 修复侧边栏样式 + 更新依赖
```

### 3. 提交前自检清单

- [ ] `git status` 确认只包含当前项目的文件
- [ ] `npm run type-check` TypeScript 类型检查通过
- [ ] `npm run build` 构建成功
- [ ] 提交信息包含对应的 Phase/F 编号，方便回溯

### 4. 消息写作规范

- **描述用中文**，与项目注释语言一致
- **动词用现在时**："新增"而不是"新增了"，"修复"而不是"修复了"
- **首字母小写**：`feat(cms): 新增登录页面`（不是"新增登录页面"）
- **不超过 72 字符**：单行描述控制在 72 字符以内

```
# 正确
feat(cms-frontend): F1.3 Axios 封装与请求/响应拦截器

# 错误
feat(cms-frontend): F1.3 Axios封装和请求拦截器还有响应拦截器和错误处理（太长）
```

## PowerShell 环境适配

Windows PowerShell 下执行提交：

```powershell
# 单行提交（推荐）
git commit -m "feat(cms-frontend): F1.1 Vite + Vue 3 + TypeScript 脚手架搭建"

# 多行提交（需要详细说明时）
git commit -m "feat(cms-frontend): F1.3 Axios 封装" `
          -m "- 创建 src/utils/http.ts Axios 实例" `
          -m "- 配置请求拦截器自动注入 JWT Token" `
          -m "- 配置响应拦截器统一错误处理"
```

## Phase 完成标记

每个 Phase 完成后，更新 `.cursor/rules/knowledge-map.mdc` 中的进度追踪表，并提交：

```
docs(cms-frontend): Phase 1 完成，更新知识地图进度追踪
```

## 提交示例汇总

```bash
# Phase 1：骨架搭建
git commit -m "feat(cms-frontend): F1.1 Vite + Vue 3 + TypeScript 脚手架搭建"
git commit -m "feat(cms-frontend): F1.2 Element Plus 容器布局与侧边栏导航"
git commit -m "feat(cms-frontend): F1.3 Axios 封装与请求/响应拦截器"
git commit -m "feat(cms-frontend): F1.4 Vue Router 嵌套路由与导航守卫"
git commit -m "feat(cms-frontend): F1.5 Pinia 用户 store 与应用 store"

# Phase 2：管理员与权限
git commit -m "feat(cms-frontend): F2.1 登录页面表单验证与 JWT Token 存储"
git commit -m "feat(cms-frontend): F2.2 管理员列表表格展示与分页搜索"
git commit -m "feat(cms-frontend): F2.3 管理员新增/编辑表单对话框与校验规则"
git commit -m "feat(cms-frontend): F2.4 角色管理 CRUD 与权限分配"

# Phase 3：内容管理核心
git commit -m "feat(cms-frontend): F3.1 栏目管理树形表格与拖拽排序"
git commit -m "feat(cms-frontend): F3.2 文章管理富文本编辑器与发布状态"
git commit -m "feat(cms-frontend): F3.3 文章详情封面图上传与标签选择"
git commit -m "feat(cms-frontend): F3.4 产品管理 CRUD 与多图片上传"
git commit -m "feat(cms-frontend): F3.5 ECharts 数据看板统计卡片"

# Phase 4：完善与收尾
git commit -m "feat(cms-frontend): F4.1 操作日志表格与筛选功能"
git commit -m "feat(cms-frontend): F4.2 系统设置配置页面"
git commit -m "feat(cms-frontend): F4.3 404/403/500 错误页面"
git commit -m "docs(cms-frontend): F4.4 Phase 4 完成，更新知识地图进度追踪"

# 其他类型示例
git commit -m "fix(cms-frontend): 修复管理员编辑对话框表单验证未重置问题"
git commit -m "refactor(cms-frontend): 提取 usePagination composable 复用分页逻辑"
git commit -m "style(cms-frontend): 调整登录页面背景渐变与卡片阴影"
git commit -m "chore(cms-frontend): 升级 element-plus 至 v2.9.6"
git commit -m "test(cms-frontend): F2.2 新增管理员列表组件单元测试"
```