---
description: "Vue组件设计规范：Composition API结构、Props设计、状态管理"
globs: ["**/*.vue"]
---

# Vue组件设计规范

##1.组件文件结构

每个`.vue`文件按以下顺序组织：

```vue
<script setup lang="ts">
//---导入---
import {ref,computed} from 'vue'
import {ElMessage} from 'element-plus'

//---Props&Emits---
const props = defineProps<{...}>()
const emit = defineEmits<{...}>()

//---响应式状态---
const loading = ref(false)
const dataList = ref<Item[]>([])

//---计算属性---
const filteredList = computed(()=>...)

//---生命周期---
onMounted(()=>{fetchData()})

//---方法---
async function fetchData(){...}
function handleSubmit(){...}
</script>

<template>
<!--模板-->
</template>

<style scoped lang="less">
/*样式*/
</style>
```

##2.组件职责规则

-**单一职责**：一个组件只做一件事，做多了就拆分
-**页面组件**(`views/`)：处理路由级逻辑、数据获取、状态协调
-**UI组件**(`components/`)：纯展示，通过props接收数据，emit事件向上通信
-**业务组件**：可复用的业务逻辑块，最小化样式

##3.Props设计规范

```vue
<script setup lang="ts">
//正确-明确区分必填和可选(含默认值)
withDefaults(defineProps<{
title:string//必填prop
items?:Item[]//可选prop(默认空数组)
loading?:boolean//可选prop(默认false)
}>(),{
items:()=>[],
loading:false,
})
</script>
```

##4.状态管理决策树

```
状态是否被多个不相关的组件使用？
是→Pinia store
否→是否跨超过2层传递？
是→Provide/Inject或Pinia store
否→局部ref()或父组件prop传递(最多2层)
```

##5.事件通信规范

```vue
<!--子组件emit带类型的事件-->
<script setup lang="ts">
const emit = defineEmits<{
(e:'submit',data:FormData):void
(e:'cancel'):void
}>()
</script>

<!--父组件监听-->
<ChildForm @submit="handleSubmit" @cancel="handleCancel"/>
```

##6.Composables模式

将可复用逻辑提取为composables：

```typescript
//src/composables/usePagination.ts
export function usePagination(fetchFn:(params:PageParams)=>Promise<PageResult>){
const pageNum = ref(1)
const pageSize = ref(10)
const total = ref(0)
const dataList = ref<unknown[]>([])

async function loadData(){
const res = await fetchFn({pageNum:pageNum.value,pageSize:pageSize.value})
dataList.value = res.records
total.value = res.total
}

function handlePageChange(page:number){
pageNum.value = page
loadData()
}

return{pageNum,pageSize,total,dataList,loadData,handlePageChange}
}
```