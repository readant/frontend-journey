# 🕊️ 光遇知识星盘 · 设计文档

> 一份完全独立的设计说明：无论你是否了解本仓库，都可以通过本文理解星盘的**设计理念、视觉与交互规范、技术实现、数据模型**，以及如何扩展它。

---

## 1. 背景与定位

前端学习最大的痛点是「**学了就忘，忘了不知道去哪查**」——尤其记不住属性名、方法名的英文拼写。传统目录式导航要求你先知道「知识点被归在哪个目录」，而星盘把这个问题反过来：

> **忘记知识点放在哪？点一颗星。**

光遇星盘是三层学习架构中**参考层（3-Reference）的灵魂与核心导航**，取代传统列表式目录，把整个前端知识体系画成一片夜空：

| 星盘隐喻 | 对应知识体系 |
| :--- | :--- |
| 星座分区 | 四大技术域（HTML / CSS / JavaScript / 工程化） |
| 星星 | 知识点（按层级分为大/中/小星） |
| 星轨连线 | 知识点之间的关联关系 |
| 光之翼面板 | 选中知识点的速查详情 |

---

## 2. 设计理念

### 2.1 中文优先原则（不可妥协）

- 星星节点上**永远只显示中文**，英文拼写只出现在两处：
  - 悬停 Tooltip（快速瞄一眼）
  - 点击后的「光之翼」详情面板（配**音节拆分 + 中文谐音助记**，专治拼写困难）
- 搜索支持**中文大白话**：输入「横着排」「居中」「必填」等口语化描述，即可定位到对应知识点。
- 每个节点都携带**口语化标签**（`tags`），这是中文搜索能工作的基础。

### 2.2 分级可视化（降低认知负荷）

知识不是平的，星星的**大小 + 亮度**直接传达知识层级：

| 层级 | 星星 | 示例 | 视觉特征 |
| :--- | :--- | :--- | :--- |
| 1 大星星 | 核心模块 | 语义化 / 弹性布局 / JS 核心 / 版本控制 | 暖黄色 + 大光晕 |
| 2 中星星 | 关键概念 | 闭包 / Promise / 盒模型 / 定位 | 星座主色 |
| 3 小星星 | 具体属性/坑点 | align-items / z-index / placeholder | 星座主色 + 微微闪烁 |

### 2.3 布局即知识地图

四大星座固定分居四象限，方位记忆天然关联：

```
┌─────────────┬─────────────┐
│ 🟦 晨岛      │ 🟩 云野      │
│  HTML（左上）│  CSS（右上） │
├─────────────┼─────────────┤
│ 🟨 雨林      │ ⬛ 暮土      │
│ JS（左下）   │ 工程化（右下）│
└─────────────┴─────────────┘
```

---

## 3. 视觉设计

### 3.1 夜空背景

- **基底**：深蓝紫径向渐变（`#1b2a6b → #141b4d → #0a0e2a → #060a1e`），模拟《光遇》的静谧夜空。
- **动态粒子**：Canvas 2D 绘制 60–90 颗萤火粒子，**缓慢向上飘浮 + 正弦闪烁**；粒子数量按容器面积自适应（`面积 / 9000`），上限 90，保证低端设备流畅。
- 星盘整体是**固定深色场景**，不随 VitePress 明暗主题切换（它是「一片夜空」，不是普通正文）。

### 3.2 星星本体

- 圆形 + 径向渐变高光（左上亮、右下暗），模拟立体球体。
- **大星星统一暖黄色**（`#ffd76a`），中小星星使用各自星座主色。
- 中文标签置于星星正下方，悬停时文字提亮发光。
- 每颗星星的呼吸动画**随机延迟与周期**（2.6s–4.2s），避免整片星空同频僵硬。

### 3.3 四大星座配色

| 星座 | 技术域 | 主色 | 光晕 | 中心恒星 |
| :--- | :--- | :--- | :--- | :--- |
| 🟦 晨岛 | HTML | `#6ea8ff` 蓝 | `rgba(110,168,255,.85)` | 语义化 |
| 🟩 云野 | CSS | `#34d399` 绿 | `rgba(52,211,153,.85)` | 弹性布局 |
| 🟨 雨林 | JavaScript | `#fbbf24` 黄 | `rgba(251,191,36,.85)` | JS 核心 |
| ⬛ 暮土 | 工程化 | `#a78bfa` 紫 | `rgba(167,139,250,.85)` | 版本控制 |

### 3.4 星轨连线

- 发光细线，`stroke-dasharray: 4 14` + 无限 `stroke-dashoffset` 动画 → 视觉上是**光点沿虚线流动**。
- 每条线使用**起点星星所在星座**的颜色，从大星星流向小星星。
- 聚焦时：与选中星星相连的线高亮加粗，无关的线几乎消失。

---

## 4. 交互设计

| 操作 | 行为 |
| :--- | :--- |
| 默认 | 星星缓慢呼吸（缩放），星轨光点缓缓流动 |
| **悬停** | 星星发出光脉冲（亮度提升），浮出 Tooltip：中文名 + 英文拼写 |
| **点击** | 星星以缓动曲线**飞向左侧中央并放大 1.55 倍**；直接相连的星星高亮脉冲，无关星星暗淡至 13%；右侧滑入「光之翼」面板 |
| **搜索** | 输入中文大白话，命中的星星**升起冲天光柱**（星座色渐变，自星位升至顶部）并提亮 |
| 再点同星 / `Esc` / ✕ | 收起详情，全盘复位 |

### 4.1 「光之翼」详情面板

点击星星后右侧弹出的速查面板，包含：

1. 星座角标（图标 + 星座名）
2. 中文名 + 英文拼写
3. 一句话中文解释
4. **音节拆分 + 中文谐音助记**（记忆专区）
5. 可运行的**代码示例**
6. 中文口语化标签（chips）
7. 通往参考层三大模块的深挖链接

小屏（≤720px）下面板退化为底部抽屉，星星飞向容器中心。

---

## 5. 技术实现

### 5.1 技术栈

| 层 | 技术 |
| :--- | :--- |
| 组件框架 | Vue 3 `<script setup lang="ts">`，注册为 VitePress 全局组件，Markdown 中 `<StarMap />` 直接使用 |
| 背景粒子 | Canvas 2D + `requestAnimationFrame` 逐帧绘制 |
| 星轨连线 | 内联 SVG（`<line>` + CSS 虚线流动动画） |
| 星星/面板/Tooltip | HTML + CSS（绝对定位 + transform + 过渡动画），原生 DOM 事件驱动交互 |
| 尺寸自适应 | `ResizeObserver` 监听容器（容器初始尺寸为 0 时也能在就绪后自动重算） |
| SSG 兼容 | 外层 `<ClientOnly>` 包裹，构建期渲染加载占位，运行时才挂载 |

### 5.2 渲染分层（z-index）

```
0  Canvas 粒子背景（pointer-events: none）
1  SVG 星轨连线（pointer-events: none）
2  星星本体（大星星 3）
4  搜索光柱
6  顶栏（搜索框 / 返回星空）
8  悬停 Tooltip
10 光之翼详情面板
```

### 5.3 核心实现要点

- **坐标体系**：数据里存百分比坐标（0–100），组件挂载时按容器像素换算，存进响应式 `pos` 表；`ResizeObserver` 触发重算。
- **聚焦飞行**：星星默认 `translate(-50%,-50%)` 居中锚定；聚焦时在同一条 transform 上叠加 `translate(目标位移) scale(1.55)`，配合 `cubic-bezier(.22,1,.36,1)` 0.8s 缓动，实现「飞向中央」。
- **星轨跟随**：聚焦星星的连线端点改用「飞行目标点」坐标，线会重新接到飞向中央的星星上。
- **联动判定**：`linkedIds` 集合 = 选中星 + 它的 `links` + 反链它的星星；据此划分 `is-focused / is-linked / is-dimmed` 三态。
- **搜索匹配**：200ms 防抖，对每个节点的「中文名 + 英文 + 全部口语化标签」做不区分大小写的包含匹配。
- **呼吸与悬停不冲突**：呼吸动画作用于 `transform: scale`，悬停/命中的光脉冲作用于 `filter: brightness`，两条属性互不覆盖。
- **性能**：粒子绘制随面积缩放上限 90；星星 hover 不触发重排（只用 transform/filter）；`will-change` 提前告知合成层。

---

## 6. 数据模型

数据位于 `docs/.vitepress/theme/components/star-map-data.ts`，纯数据驱动，改数据即改星盘，无需动组件代码。

### 6.1 星座接口

```ts
interface Constellation {
  id: "morning" | "cloud" | "rain" | "dusk"; // 星座唯一标识
  name: string;   // 星座中文名（晨岛/云野/雨林/暮土）
  en: string;     // 技术域（HTML/CSS/JavaScript/工程化）
  color: string;  // 主色
  glow: string;   // 光晕色（rgba）
  icon: string;   // 分区角标 emoji
  description: string; // 一句话文案
}
```

### 6.2 星星接口

```ts
interface StarNode {
  id: string;             // 唯一标识
  name: string;           // 中文显示名（节点上只显示它）
  en: string;             // 英文拼写（Tooltip / 详情面板）
  level: 1 | 2 | 3;       // 大/中/小星星
  constellation: string;  // 所属星座 id
  x: number; y: number;   // 百分比坐标 0-100
  desc: string;           // 一句话中文解释
  phonetic?: string;      // 音节拆分（e.g. "just-i-fy"）
  mnemonic?: string;      // 中文谐音助记
  code?: string;          // 代码示例
  tags: string[];         // 中文口语化标签（搜索用）
  links?: string[];       // 星轨连线（流向的子节点 id）
}
```

### 6.3 节点示例

```ts
{
  id: "flex",
  name: "弹性布局",
  en: "flexbox",
  level: 1,                        // 大星星（核心模块）
  constellation: "cloud",          // 云野
  x: 78, y: 16,                    // 右上象限
  desc: "一维布局首选：容器一行声明，项目自动排列、对齐、伸缩",
  phonetic: "flex-box",
  mnemonic: "弗莱克斯盒子 —— 会伸缩的盒子",
  code: ".parent {\n  display: flex;\n  align-items: center;\n}",
  tags: ["横着排", "竖着放", "弹性", "居中", "排均匀"],  // 中文大白话
  links: ["box-model", "grid", "position", "animation"],
}
```

---

## 7. 如何扩展

**新增一颗星星**（4 步）：

1. 在 `star-map-data.ts` 对应星座分区追加一个节点对象（照 6.3 示例填全字段）。
2. 给 `x / y` 选一个**该象限内不重叠**的百分比坐标。
3. 用 `links` 挂到它的「父」星星上，星轨自动出现。
4. `tags` 尽量多写**口语化说法**——这是中文搜索能不能找到它的关键。

**新增一个星座分区**：

1. 在 `constellations` 数组追加分区（选一个主色 + 光晕）。
2. 在 `zoneStyle`（StarMap.vue）的四象限映射里登记角标位置。
3. 新节点的 `constellation` 指向新 id。

**调整配色**：只改 `constellations` 里的 `color / glow`，全盘星星、星轨、光柱、面板角标自动跟随。

---

## 8. 文件清单

| 文件 | 职责 |
| :--- | :--- |
| `docs/.vitepress/theme/components/StarMap.vue` | 星盘组件（视觉 + 交互 + 粒子） |
| `docs/.vitepress/theme/components/star-map-data.ts` | 星盘数据（星座 + 星星 + 关联） |
| `docs/.vitepress/theme/index.ts` | 注册 `<StarMap />` 全局组件 |
| `docs/3-reference/index.md` | 全屏星盘入口（`layout: false`，嵌入 `<StarMap fullscreen />`） |
| `docs/3-reference/intro.md` | 参考层总览（手册优先结构导览） |
| `docs/3-reference/1-handbook/` | 📖 完整知识手册（按技术域系统整理，星盘导航的目标页） |
| `docs/3-reference/2-scenarios/` | 🔍 场景索引（手册的快速入口，摘要 + 链接） |
| `docs/3-reference/graph-data.json` | 精简导航数据（20 个核心节点，含 `link` 指向手册章节） |
| `docs/.vitepress/config.ts` | 导航栏「光遇星盘」+ 侧边栏「🕊️ 参考层」 |

---

## 9. 已知边界（原型阶段）

- 聚焦飞行时星轨线端点是**瞬移**到目标点的（SVG 几何属性无法走 CSS 过渡），视觉上星星先飞、线后接。
- 星盘为原型规模：目前内置 52 颗星；节点密度继续增加时，需要考虑力导向布局或分区缩放。
- 窄屏（<720px）下面板变底部抽屉、星星飞向容器中心，四象限布局在小屏上观感打折，后续可针对移动端重排象限。
